apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: vault-auth
  namespace: default
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: employee-api
    serviceAccount: employee-api-vault
    audiences:
      - vault
  vaultConnectionRef: vault-connection
